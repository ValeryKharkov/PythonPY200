Когда не определен магический метод `__bool__`, отвечающий за проверку истинности объекта,  
то вызывается магический метод `__len__` 

И если он возвращает 0 это `False`, если отличное от нуля число, то `True`.
 